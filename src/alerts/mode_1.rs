use myelin_geometry::{ConvexHull, Polygon};

/*
pub struct Envelope {
    minimum : (f64, f64),
    Polygon
   // f : FnMut(f64)->f64,
}

impl Envelope {

    pub fn new(minimum : (f64, f64), envelope: Vec<(f64,f64)>)->Self{
        Self{
minimum
        }
    }

    pub fn check(&self, input: (f64, f64))-> bool{
        if input.0< self.minimum.0 || input.1 <self.minimum.1{
            return false;
        }


        input.1 >
    }
}
*/
